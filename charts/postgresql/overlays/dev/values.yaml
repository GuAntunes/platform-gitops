# Overlay dev — apenas deltas sobre o base.
# database e nodePort ficam no delta do projeto (argocd/applications/<projeto>/dev/postgres-values.yaml).

resources:
  limits:
    cpu: 300m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
