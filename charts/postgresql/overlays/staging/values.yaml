# Overlay staging — apenas deltas sobre o base.

postgresql:
  database: app_staging
  password: postgres_staging

service:
  nodePort: 30433

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi
